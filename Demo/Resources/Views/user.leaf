#extend("base"):
  #export("content"):
	 <h1>#(user.name)</h1>
	 <h2>#(user.username)</h2>
	 #if(count(acronyms) > 0):
		#extend("acronymsTable")
	 #else:
		<h2>There aren’t any acronyms yet!</h2>
	 #endif
  #endexport
#endextend
